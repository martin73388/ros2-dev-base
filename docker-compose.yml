services:
  ros2_dev_env:
    platform: linux/arm64
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    tty: true
    volumes:
      - ./ros2_ws:/home/rosuser/ros2_ws
      - ~/.ros:/home/rosuser/.ros
    network_mode: "host"
    environment:
      - DISPLAY=${DISPLAY}